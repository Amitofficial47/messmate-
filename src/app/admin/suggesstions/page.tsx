// This page is no longer used and can be deleted.
// To prevent build errors if it's still linked somewhere unexpectedly,
// we'll return a simple component.

export default function MenuSuggestionsPage() {
  return (
    <div className="container mx-auto py-8">
      <h1 className="text-3xl font-bold text-primary">Menu Suggestions</h1>
      <p className="text-muted-foreground">This feature has been removed.</p>
    </div>
  );
}
